# ShopPilot week plan — 2026-08-25 → 2026-09-01

> **Status of this file:** **PARKED 2026-08-25 evening.** Cut quality overrides
> first-hour / joinery. Dispatch from
> [`CUT_QUALITY_RESEARCH_2026-08-25.md`](./CUT_QUALITY_RESEARCH_2026-08-25.md)
> + Hermes paste [`CUT_QUALITY_HERMES_ORCHESTRATOR_PROMPT.md`](./CUT_QUALITY_HERMES_ORCHESTRATOR_PROMPT.md).
> Next Mac card: **SPK-2100a** (engine only). Resume leftover week-plan items
> (**2023e**, optional) after PHASE Y Wave Q1 or when the owner says so.
>
> Prior Hermes paste (do not use until quality Q1 lands): [`WEEK_PLAN_HERMES_ORCHESTRATOR_PROMPT.md`](./WEEK_PLAN_HERMES_ORCHESTRATOR_PROMPT.md)
> North star: [`LEAN_CNC_SCOPE.md`](./LEAN_CNC_SCOPE.md) · Board: [`MASTER_KANBAN.md`](../../MASTER_KANBAN.md)
> Sibling Win board: `~/Desktop/VectorPilot/HERMES_KANBAN.md` (H-601…H-610; H-701 after 2100a)

**Do not stamp `SPK-0623`.** **Do not pick `SPK-1900g` license.** **Do not reopen medial-axis (SPK-2010 closed).** **`SPK-2022f` stays parked.**

---

## What already shipped (do not redo)

Mac PHASE X `[x]` as of 2026-08-25 evening: **2020a0, 2020a, 2021a, 2022a–e, 2022g, 2023a**.
Phase U a–h `[x]`. Trochoid Mac **SPK-1910** `[x]`. V-carve valley **SPK-2010** `[x]`.

Hermes already jumped the old claim order: it shipped **2023a** (chip-load) *before* first-hour presets, then attempted **2023b** (T-bones) and **died on Nous 429 / max_iterations with zero code**. Do not retry 2023b until Wave 1–2 land.

Win H-501 (material+bit presets) is `[x]`. Mac already has `MaterialBitPresetPicker` from **SPK-1920e** — **2024b extends it** (2D Cut forms + `feedsFromPreset` so 2023a stays silent). Do not rebuild a second picker.

---

## Goal for the week

Close the three remaining gaps that still lose a first-hour user, then joinery, then the family contract:

1. **Trust contract** — Sign + plaque + inlay `.shoppilot` goldens (`SPK-1920i`)
2. **First hour** — presets that silence chip-load, gallery-first welcome, one CTA per stage (`2024b → 2024a → 2024c`)
3. **Joinery** — T-bones, 2D rest pocket engine then UI, copy-along-path (`2023b → 2023c → 2023d → 2023e`)
4. **Win catch-up** — H-610 trochoid + H-601…H-606 mirrors of Mac work that already merged
5. **Hygiene** — SHAKE 1319/1320 (likely stale Package.swift path; re-run first)

Exit Friday/Monday: Mac remaining PHASE X Ready cards `[x]` except parked **2022f**; Win H-610 plus at least H-601…H-606 `[x]` or honestly `[~]` with a blocker note; `CONTRACT_GOLDENS.md` exists with hashes; parent **SPK-1920** can close when **1920i** `[x]` (j/k optional audit).

---

## Capacity (hardware reality — do not exceed)

| Lane | Profile | Max concurrent | Notes |
| --- | --- | --- | --- |
| Mac coding | Hermes `coder` (Nous) | **3** | Swift serialized by `./scripts/swift_locked.sh` |
| Win coding | Hermes `coder` on VectorPilot | **2** | No Mac Swift lock; still cap at 2 so the Mac lock stays free |
| Spark / local Qwen | off | **0** | Do not enable unless a card is docs-only with **zero** `swift` |

8–16 agents on this Mac is anti-throughput. **Orchestrator implements nothing.** It claims, spawns ≤5 workers (3 Mac + 2 Win), waits, flips boards, spawns the next wave.

Every Mac card: `--max-runtime 60m` (90m only for 2023c rest engine). Worktree. All Swift via lock. Never `rm -rf .build`. Never `swift test`. Gate = `./scripts/verify_locked.sh ShopPilotVerifyXXXX` (or the python/AX gate named on the card).

---

## File-lock matrix (serialize these)

| Hot file / area | Cards that own it | Rule |
| --- | --- | --- |
| `ContentView.swift`, `WelcomeSheetView.swift`, `CoachPanelView.swift` | 2024a, 2024c | **Serialize:** a then c |
| `SpecialtyParamsForms.swift` | 2024b, 2023d, 2023b UI | **One at a time** |
| `PocketToolpath.swift` | 2023c then 2023d | **c `[x]` before d starts** |
| `Dogbone.swift` | 2023b | Solo |
| `ArrayCopy.swift` + Design ops bar | 2023e | After 2023b/c/d |
| `AppSession.swift` save/open | 1920i | Solo vs 2024a if both would touch session load |
| SHAKE 1319 / 1320 | verify targets only | Parallel-ok with anything |
| VectorPilot `src/**` | H-601…H-610 | Parallel with **all** Mac cards |

---

## Waves (spawn the whole wave, wait, then the next)

### Wave 1 — start now (3 Mac + 2 Win)

| Slot | Card | Why this wave |
| --- | --- | --- |
| MAC1 | **SPK-1920i** | Skipped claim-order item. Sign + 3D plaque + **inlay** (2021a merged) `.shoppilot` save/reopen hashes → `docs/planning/CONTRACT_GOLDENS.md` |
| MAC2 | **SPK-2024b** | 2023a already nags default-1000 feeds. Named combo (Walnut 18 mm + 90° V-bit) fills Cut depth/feed/rpm; sets `feedsFromPreset`; Advanced still shows every field. **Audit 1920e picker first.** |
| MAC3 | **SPK-SHAKE-BUG-ShopPilotVerify1319** | Re-run first. Aug-24 log was `invalid custom path ShopPilotVerify0109` — that target now exists. If PASS at HEAD, audit-close. If FAIL, product vs harness, then fix. |
| WIN1 | **H-610** | Mac-only trochoid (SPK-1910) still has no Win registry. Family rule: no more Mac-only strategies. |
| WIN2 | **H-601** | Mirror Mac 2020a0+2020a (join tolerance 0.1 mm + doctor Apply buttons + V-carve Fix). Copy semantics, not Swift. |

### Wave 2 — after Wave 1 Mac 1920i + 2024b are `[x]`

| Slot | Card |
| --- | --- |
| MAC1 | **SPK-2024a** — Welcome = sample gallery as the **first** screen. `WelcomeSheetView` already lists `SampleProjectsStore` behind `FirstRunGate` (once per machine). Gap is landing-view vs one-shot sheet + single "Plan the cuts" CTA. Reuse SPK-1403 loader. Audit-first. |
| MAC2 | **SPK-2023c** — 2D rest **engine** on `PocketToolpath.swift`. `previousToolDiameterMm > 0` machines only leftover; `0` = byte-stable today. No form on this card. |
| MAC3 | **SPK-SHAKE-BUG-ShopPilotVerify1320** — same diagnosis pattern as 1319. |
| WIN1 | **H-602** inlay physics (copy Mac 2021a numbers verbatim: tip 0.1 / glue 0.05 pocket-out-plug-unchanged / fudge 1.002) |
| WIN2 | **H-603** device profile library (six-machine JSON, Generic fallback) |

### Wave 3 — after 2024a `[x]` and 2023c `[x]`

| Slot | Card |
| --- | --- |
| MAC1 | **SPK-2024c** — one primary forward CTA per stage; coach strip names that action. Audit-first (close as audit if already true). |
| MAC2 | **SPK-2023b** — T-bones. Bit Ø only; along-X / along-Y / auto-longest-edge. Existing dogbone fixtures **byte-stable**. Yesterday's attempt wrote nothing — start clean. |
| MAC3 | **SPK-2023d** — rest fields on Pocket (+ V-clearance if valley left wide stock). Deps: 2023c. |
| WIN1 | **H-604** per-op enable + send filter (mirror 2022e) |
| WIN2 | **H-605** macros + alarm decode (mirror 2022g) **or** **H-606** chip-load (mirror 2023a, share `bit_feeds_seed.json`) — pick whichever is thinner; the other is Wave 4. |

### Wave 4 — close-out

| Slot | Card |
| --- | --- |
| MAC1 | **SPK-2023e** — copy along path (N or spacing, tangent-follow). After 2023b/c/d. |
| MAC2 | **SPK-1920j** and **SPK-1920k** — audit-close or `[-]` with a note. Do not invent a split/fade or a 4th-axis product. |
| MAC3 | Flip **SPK-1920** parent `[x]` iff a–i are `[x]`. Do **not** touch 0623. |
| WIN1 | Remaining of H-605/H-606 |
| WIN2 | **H-607** only if 2023b `[x]`; **H-608** only if 2023c+d `[x]`; **H-609** only if 2023e `[x]` |

If a Win card's Mac twin is not `[x]` yet, skip it and take the next Ready H-card whose twin *is* `[x]`.

---

## Day map (wall-clock, not a promise)

| Day | Calendar | Expected `[x]` |
| --- | --- | --- |
| 0 | Tue 8/25 eve | Wave 1 spawned |
| 1 | Wed 8/26 | 1920i, 2024b, SHAKE-1319, H-610, H-601 |
| 2 | Thu 8/27 | 2024a, 2023c, SHAKE-1320, H-602, H-603 |
| 3 | Fri 8/28 | 2024c, 2023b, 2023d, H-604, H-605 or H-606 |
| 4 | Sat 8/29 | 2023e, 1920j/k audit, remaining H-606…H-608 |
| 5 | Sun 8/30 | Slack / 429 retry / Win H-609 |
| 6 | Mon 9/01 | Wave-close: parent 1920, work logs, no 0623 |

Slippage: if Nous 429s (happened today on 2023b), **do not spawn replacements on top**. Heartbeat, wait, retry the same card once, then pick the next Ready orthogonal card.

---

## Card briefs (Mac)

Each child already lives on `MASTER_KANBAN.md`. This section is the AC the worker must hit.

### SPK-1920i — contract goldens
- Save Sign sample + 3D plaque + one inlay job as `.shoppilot` fixtures.
- Reopen on Mac: vectors, toolpaths, relief, inlay pocket+plug params intact.
- Write `docs/planning/CONTRACT_GOLDENS.md` with file hashes so VectorPilot can verify. Do not run VectorPilot tests from the Mac card.
- Verify: reopen asserts + hash file exists. `./scripts/swift_locked.sh build --target ShopPilot` only if a loader change is required.

### SPK-2024b — presets over parameters
- Named preset "Walnut 18 mm + 90° V-bit" fills Cut **depth / feed / rpm** on Profile/Pocket/V-Carve (not only Rough3D).
- Sets `feedsFromPreset = true` so `ShopPilotVerify2023a` AC5 stays silent.
- Advanced disclosure still shows every field.
- **Out of scope:** new Tool DB schema, chip-load math (2023a closed).
- Gate: `./scripts/verify_locked.sh ShopPilotVerify2024b`.

### SPK-2024a — welcome gallery first screen
- Four samples from `SampleProjectsStore` are the landing view (no second catalog).
- One click → Design + single **"Plan the cuts"** CTA. Import Artwork secondary.
- Preserve SPK-1403 `SampleProjectLoader` hooks.
- **Out of scope:** new samples, NavigationSplitView rewrite.
- Gate: update `scripts/verify_1603_welcome.py` if present + AX walk row; app target build.

### SPK-2024c — one forward CTA per stage
- Setup / Design / Cut / Preview / Machine: exactly one primary next-action.
- Coach strip promotes that same action.
- Audit-first: if a stage already complies, document call-site and close.
- Gate: AX walk rows + python grep gate.

### SPK-2023c — 2D rest engine
- `PocketToolpath` `previousToolDiameterMm`; `> 0` → leftover only; `0` → byte-identical to current goldens.
- Golden: 1/4″ then 1/16″ pocket → G1 inside leftover band; floor covered once.
- **No UI on this card.**
- Gate: `ShopPilotVerify2023c`. `--max-runtime 90m`.

### SPK-2023d — rest UI (deps 2023c)
- Field + previous-tool picker on Pocket form (and V-carve flat-clearance if applicable).
- Gate: `ShopPilotVerify2023d`.

### SPK-2023b — T-bones
- Bit Ø only prompt; along-X / along-Y / auto-longest-edge.
- `Dogbone.swift` T-notch variant. Dogbone fixtures byte-stable.
- Gate: `ShopPilotVerify2023b`.

### SPK-2023e — copy along path
- N copies or spacing along a curve; tangent-follow toggle; one undo.
- Generalize `ArrayCopy.swift`. After 2023b/c/d.
- Gate: `ShopPilotVerify2023e`.

### SHAKE 1319 / 1320
- Re-run `./scripts/verify_locked.sh ShopPilotVerify1319` (resp. 1320) at current HEAD.
- 2026-08-24 failure was Package.swift missing `Sources/ShopPilotVerify0109`. Re-audit: if green, `[x]` with "stale harness" work log. If red, fix product or verify, then nearest regression.

### Parked / human-only (do not spawn)
- **SPK-2022f** resume-at-line-N — parked. Safe-entry state machine is its own card later.
- **SPK-0419** live air-cut — `[!]`.
- **SPK-0623** owner ship gate.
- **SPK-1900g** license choice.

---

## Card briefs (Win)

Gate: `./verify.sh` filter, Release, zero warnings. Copy Mac numbers; do not port Swift.

| ID | Mirror of | Slice |
| --- | --- | --- |
| H-610 | SPK-1910 | Register trochoid slot op; Mac-identical WOC/pitch/ramp semantics; Cut menu + params round-trip |
| H-601 | 2020a0+a | Join tolerance 0.1 mm on freehand; doctor Apply; V-carve Fix |
| H-602 | 2021a | tip 0.1 / glue 0.05 pocket-out / fudge 1.002; numeric goldens |
| H-603 | 2022d | Same six-machine catalog JSON |
| H-604 | 2022e | Per-op enabled; send filter; re-enable byte-stable |
| H-605 | 2022g | Macros, no auto-run; GRBL 1.1 ALARM banner |
| H-606 | 2023a | Same `bit_feeds_seed.json` ranges; preset-trusted silent |
| H-607 | 2023b | Only after Mac 2023b `[x]` |
| H-608 | 2023c+d | 2D pocket rest, UI required before `[x]` |
| H-609 | 2023e | Only after Mac 2023e `[x]` |

---

## Explicitly do not build this week

the incumbent suite tourism · LightBurn/Fusion traps · App Store / notarize · laser as a product · more strategy rows · medial-axis rewrite · 2022f resume · 8–16 parallel agents · `swift test` · `rm -rf .build`.
